import Modal, {
    ModalBody,
    ModalFooter,
    ModalHeader,
    ModalTitle,
    ModalTransition,
  } from "@atlaskit/modal-dialog";
  
  import Button from "@atlaskit/button/standard-button";
  import { ACTION } from "../../../consts/action.const";
  
const ModalComponent = (isOpen, action = '',createTemplate, editTemplate, deleteTemplate, submit) => {
    return ( <div>

    </div> );
}

export default ModalComponent;